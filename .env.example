# =============================================================================
# AI Job Hunter - 环境变量配置
# 位置：/opt/project/ai-job-hunter/.env
# 部署：chmod 600 .env
# =============================================================================

# ---------------------------------------------------------------------------
# 数据库（公用 PostgreSQL；与 cogniforge 工程统一一套变量名，
# 服务器 db.env 已有 PGSQL_HOST/PGSQL_PORT/PGSQL_USERNAME/PGSQL_PASSWORD/PGSQL_DB 等）
# 参考：~/.cursor/skills/deploy/SKILL.md §3.2、cogniforge/configs/config.yaml
# ---------------------------------------------------------------------------
PGSQL_HOST=your-postgres-host
PGSQL_PORT=5432
PGSQL_USERNAME=postgres
PGSQL_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD
PGSQL_DB=job_hunter
PGSQL_SSLMODE=disable

# ---------------------------------------------------------------------------
# Redis（db0 + 命名空间隔离；与 cogniforge 一致）
# ---------------------------------------------------------------------------
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ---------------------------------------------------------------------------
# GitHub Container Registry
# ---------------------------------------------------------------------------
IMAGE_NAMESPACE=your-github-username

# ---------------------------------------------------------------------------
# 镜像版本（CD 自动更新，永远保持 latest）
# ---------------------------------------------------------------------------
IMAGE_TAG=latest

# ---------------------------------------------------------------------------
# 应用域名（Traefik 路由用）
# ---------------------------------------------------------------------------
DOMAIN=ai-job-hunter.example.com

# ---------------------------------------------------------------------------
# JWT 密钥（生成：openssl rand -base64 32）
# ---------------------------------------------------------------------------
JWT_SECRET=CHANGE_ME_TO_SECURE_JWT_SECRET

# ---------------------------------------------------------------------------
# 加密密钥（生成：openssl rand -hex 32）
# ---------------------------------------------------------------------------
ENCRYPTION_KEY=CHANGE_ME_TO_32_BYTE_ENCRYPTION_KEY

# ---------------------------------------------------------------------------
# DeepSeek API Key
# ---------------------------------------------------------------------------
DEEPSEEK_API_KEY=sk-change-me

# ---------------------------------------------------------------------------
# Spring AI 配置（命名与 application.yml 保持一致，便于查阅）
# Spring Boot relaxed binding: DEEPSEEK_BASE_URL → spring.ai.deepseek.base-url
# 模型可选：deepseek-chat | deepseek-reasoner | deepseek-v4-pro 等
# ---------------------------------------------------------------------------
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_CHAT_MODEL=deepseek-chat
DEEPSEEK_CHAT_TEMPERATURE=0.7
